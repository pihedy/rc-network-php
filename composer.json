{
    "name": "pihedy/rc-network-php-server",
    "description": "The server that controls the RC devices.",
    "type": "project",
    "autoload": {
        "psr-4": {
            "App\\": "src/app/"
        }
    },
    "config": {
        "vendor-dir": "src/vendor"
    },
    "require": {
        "php": ">=8.1.0"
    }
}
